[build-system]
requires = ["setuptools>=42", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "tree-sitter-kotlin"
description = "Brokk-maintained Kotlin grammar for tree-sitter"
version = "0.4.4"
keywords = ["incremental", "parsing", "tree-sitter", "kotlin"]
classifiers = [
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Topic :: Software Development :: Compilers",
  "Topic :: Text Processing :: Linguistic",
  "Typing :: Typed",
]
requires-python = ">=3.9"
license.text = "MIT"
readme = "README.md"

[project.urls]
Homepage = "https://github.com/BrokkAi/tree-sitter-kotlin"
Repository = "https://github.com/BrokkAi/tree-sitter-kotlin"
Issues = "https://github.com/BrokkAi/tree-sitter-kotlin/issues"

[project.optional-dependencies]
core = ["tree-sitter~=0.22"]

[tool.cibuildwheel]
build = "cp39-*"
build-frontend = "build"
